# Third-party licenses

The `dovi_tool` and `hdr10plus_tool` binaries are bundled inside RedSync and are
redistributed under the MIT License (see the files here). `audio-offset-finder`
(Apache-2.0) is not bundled; RedSync's offset engine follows its approach only.
See the top-level NOTICE for the full attribution list.
